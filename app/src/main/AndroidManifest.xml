<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ibmhackathon2020">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Orders"></activity>
        <activity android:name=".OrderItem" />
        <activity android:name=".SellerAddDetails" />
        <activity android:name=".CustomerAddDetails" />
        <activity android:name=".CartActivity" />
        <activity android:name=".DisplayItemsForCustomer" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".DisplayItems" />
        <activity android:name=".Add_items" />
        <activity android:name=".WatsonAsst" />
        <activity
            android:name=".Admindashboard"
            android:label="@string/title_activity_admindashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".login_activity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="$(applicationId).here.this.library.provider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

</manifest>